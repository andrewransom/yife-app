# Query Composables

Server content reads belong here and should use TanStack Query.

Components must not call Supabase directly. After generated database types exist, query composables can call app-owned service/client wrappers.
